# 安全策略

## 支持的版本

| 版本 | 是否接受安全修复 |
| --- | --- |
| 0.3.x | ✅ |
| < 0.3 | ❌ |

## 报告漏洞

请**不要**用公开 issue 报告安全问题。请通过 GitHub 仓库的
Security → Report a vulnerability（私有漏洞报告）提交，或在仓库 issue 列表
中开一个不带复现细节的 issue，请求维护者私下联系你。

请在报告中包含：影响版本、复现步骤、期望与实际行为、以及你判断的影响面。

## 项目内的安全红线

本项目的定位是**授权范围内的**负载测试工具，代码层面明确了以下红线：

1. **必须显式声明授权**。`StartRunRequest.authorized` 为 `false` 时，引擎
   直接拒绝启动（`CoreError::NotAuthorized`），不做任何联网动作。
2. **不接受非 HTTP(S) 目标**。URL 必须 `http://` 或 `https://` 开头。
3. **不设本地服务端口**。桌面壳是原生窗口 + IPC，不监听任何 TCP 端口。
   若你观察到该进程在监听端口，那一定不是本版本的二进制。
4. **凭据绝不入库**。`.gitignore` 已排除 `api.txt` / `.env*` / `*.key`。
   历史事故：曾有明文 API key 被误放进 `api.txt`，该文件已删除，且已被
   `.gitignore` 永久拦截。

## 如果你泄露了密钥

按顺序做，别只删文件（文件删了密钥仍然有效）：

1. 到平台**吊销/轮换**该密钥（删除文件不等于密钥失效）。
2. 用 `git log -p -- api.txt` 类命令排查是否已进入提交历史。
3. 若已进入历史，用 `git filter-repo` 重写历史并强制推送，同时通知协作者重新克隆。
